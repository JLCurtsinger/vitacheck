import { supabase, supabaseUrl, supabaseAnonKey } from '@/lib/supabase';

// Re-export the canonical client and config so existing imports keep working.
export { supabase, supabaseUrl, supabaseAnonKey };
